FROM node:22-bullseye-slim

WORKDIR /app

ENV NODE_ENV=development
# On some host OS watchers, polling is required for reliable file change detection
ENV CHOKIDAR_USEPOLLING=true

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
